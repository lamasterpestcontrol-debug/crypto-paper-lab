{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.discovery"
  },
  "deploy": {
    "startCommand": "python -u launcher.py"
  }
}
